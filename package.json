{
  "name": "elastic-id-poster",
  "version": "1.0.0",
  "description": "A simple app that takes in a list of hapi-fhir ids that are known to be missing in the elastic fhir-raw-* index and then do a lookup on the resource in hapi-fhir to repost into kafka",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "author": "Arran Standish",
  "license": "ISC",
  "dependencies": {
    "kafkajs": "^2.2.4",
    "pg": "^8.11.3"
  }
}
